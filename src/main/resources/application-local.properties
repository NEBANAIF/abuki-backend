# ── Local Development Overrides ────────────────────────────────────────────────
# Use with: mvn spring-boot:run -Dspring.profiles.active=local
# This connects directly to MySQL running on host machine

spring.datasource.url=jdbc:mysql://localhost:3306/stokio_db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=pass123
spring.jpa.show-sql=true
logging.level.com.abuki=DEBUG
